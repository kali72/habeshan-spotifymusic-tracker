- name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install spotipy gspread oauth2client requests beautifulsoup4
name: Run Habesha Spotify Tracker Scraper

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
